# co_task

For running the control application, call `run.py`, which will read user commands interactively.
In order to load a preset sequence of commands from a file, pass the file as argument (example file provided):

     python run.py assets/input.txt

Also included is a minimal testing script w/ unit tests for the code.

![build status](https://github.com/joaocandre/co_task/actions/workflows/test.yml/badge.svg?event=push)
